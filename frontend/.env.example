# Base URL of the Sentinel API, including the version prefix.
# Local backend (see ../docker-compose.yml): http://localhost:8000/api/v1
# Production: https://<your-backend-host>/api/v1
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
